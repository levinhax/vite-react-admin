import './index.less'

import React, { useEffect, useState } from 'react'

function Page() {
  const [msg, setMsg] = useState('hello')

  useEffect(() => {
    setMsg(`hello: ${import.meta.env.VITE_APP_TITLE}`)
  }, [])

  return (
    <div className="{{lowerCase name}}-wrapper">
      <h1>{{upperCase name}} 页面</h1>
      <h2>{msg}</h2>
    </div>
  )
}

export default Page
